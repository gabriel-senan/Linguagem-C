#include <stdio.h>

char nome_pessoa[20]; //Solução letra (a)
float nro;			// Solução letra (b)
char flag;			// Solução letra (c) armazena valor V ou F
int vlr_int; 		// Solução letra (d)
float vlr_dec; 		// Solução letra (e)
int vlr_neg;		// Solução letra (f)
float vlr_frac;		// Solução letra (g) 
float vlr_frac_h;   // Solução letra (h)
int vlr_int_grande; // Solução letra (i)
int vlr_exp; 		// Solução letra (j)
float vlr_frac_grande;	// Solução letra (k)

main() {
	
}
